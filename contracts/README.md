# Afterna API contracts

- [`openapi.yaml`](./openapi.yaml) — client-facing routes
- [`fixtures/`](./fixtures) — canned CanonicalTranscript, summary, Ask, job status for iOS mocks

iOS `ConversationCore` decodes these shapes. Backend fixture mode returns equivalent payloads.
